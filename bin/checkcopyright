#!/bin/sh

[ $# -eq 0 ] && set "$PWD"

for directory in "$@"
do
    find "$directory" -name "*.java" | while read source
    do
	if grep "\$Header:" $source > /dev/null
	then
	else
	    echo $source
	fi
    done # end for
done # end for
